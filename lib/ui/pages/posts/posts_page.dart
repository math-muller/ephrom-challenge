import 'package:flutter/material.dart';

import 'posts_presenter.dart';

class PostsPage extends StatelessWidget {
  final PostsPresenter presenter;

  PostsPage(this.presenter);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
